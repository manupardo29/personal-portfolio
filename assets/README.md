# Assets

Place Manuel Pardo's final CV PDF here:

```text
assets/Manuel_Pardo_CV_EN.pdf
```

The portfolio links to that file from the "Download CV" buttons. Replace the filename in `index.html` if the final PDF uses a different name.
